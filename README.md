# Image-classifer-for-waste-image-sorting
i build an image classifier for sorting waste images using different ResNet networks. Evaluated the performances of each model using different measures and matrices.
